package context.catalog.command_side.commands.handler

import context.catalog.command_side.commands.generic.CommandHandler
import context.catalog.command_side.commands.AddMetadata

class AddMetadataHandler(): CommandHandler<AddMetadata> {

  override fun handle(command : AddMetadata) {
    TODO("Not yet implemented")
  }

}