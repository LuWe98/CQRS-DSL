package CQRS.Generation.generation;

/*Generated by MPS */

import java.util.Map;
import jetbrains.mps.internal.collections.runtime.MapSequence;
import java.util.HashMap;

public class DependencyTextConverters {

  private static final Map<GenerationTarget, DependencyTextConverter> converterMap = MapSequence.fromMap(new HashMap<GenerationTarget, DependencyTextConverter>());

  public static DependencyTextConverter get(GenerationTarget target) {
    return MapSequence.fromMap(converterMap).get(target);
  }

  public static void register(DependencyTextConverter converter) {
    MapSequence.fromMap(converterMap).put(converter.getGenerationTarget(), converter);
  }

  public static void clearAll() {
    MapSequence.fromMap(converterMap).clear();
  }

  public static void unregister(GenerationTarget target) {
    MapSequence.fromMap(converterMap).removeKey(target);
  }

}
